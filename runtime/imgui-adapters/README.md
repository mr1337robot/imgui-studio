# Dear ImGui Adapters

Version-specific wrappers for selected `imgui_internal.h` behavior belong here. Each supported
Dear ImGui version has a narrow, tested adapter. Direct project internal use remains allowed but
must be reported as a portability concern.
