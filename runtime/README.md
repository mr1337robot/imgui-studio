# Studio C++ Runtime

The runtime supplies backend-neutral interaction, animation, inspection, asset, clock, and
portable drawing APIs shared by browser and native targets.

Phase 0 contains only the versioned C++20 contract target. Runtime behavior begins in Phase 3 and
must conform to `RUNTIME_API.md`, `ANIMATION_SPEC.md`, and `INSPECTION_PROTOCOL.md`.
