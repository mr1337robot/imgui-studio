# Studio Local Service

The local service will own project confinement, atomic revisions, build coordination, immutable
artifacts, preview delivery, and export assembly. It exposes the canonical `/api/v1` contracts.

Implementation begins in Phase 2. Read `AGENT_TOOL_API.md`, `PROJECT_FORMAT.md`, and
`SECURITY_MODEL.md` before adding code here.
