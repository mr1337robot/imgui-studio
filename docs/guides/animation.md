# Animation Guide

The deterministic animation runtime begins in Phase 3. `ANIMATION_SPEC.md` is the normative
contract. This guide will contain runnable tween, spring, reset, replay, and filmstrip examples as
those APIs are implemented.
