# Tests

The test suite follows `TEST_PLAN.md`. Phase 0 provides strict schema fixtures and a C++20 CTest
foundation. Later phases add runtime unit, browser, native parity, visual, security, performance,
and agent evaluation lanes.

Run the current complete gate with `npm run validate` from a pinned Windows developer environment.
