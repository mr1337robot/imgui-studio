# Windows Native Parity Fixture

The Win32 plus DirectX 11 parity host is created in Phase 1. It will compile the same project
translation units as the WASM preview and produce canonical RGBA captures.
