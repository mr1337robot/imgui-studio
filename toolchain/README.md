# Toolchain

This directory owns reproducible toolchain configuration, safe repository-local bootstrap scripts,
CMake modules, and the release version-set manifest.

See `docs/operations/toolchain.md` for exact setup and recovery procedures.
