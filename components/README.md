# Editable Components

Source-editable custom widgets, layout helpers, and portable effects arrive in Phase 5 after the
shared rendering and deterministic inspection foundations pass. Components are examples and
forkable source, not sealed controls.
