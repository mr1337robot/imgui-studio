# Widgets

Custom widget implementations will live here. Every widget requires readable C++20 source,
stable inspection registration, relevant state coverage, deterministic scenarios, and browser/
native parity tests.
